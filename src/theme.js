import { createTheme } from "@mui/system";

export const theme = createTheme({
  palette: {
    primary: {
      main: "blue",
      light: "skyblue",
    },
    secondary: {
      main: "yellow",
    },
    otherColor: {
      main: "indigo",
    },
  },
});
